services:
  uplink-toolbox:
    image: alpine:3.20
    profiles: ["tools"]
    command: ["sh", "-c", "ip addr; ip route; ping -c 2 172.31.255.1"]
    networks:
      - uplink

networks:
  uplink:
    name: nsgw-uplink
    external: true
